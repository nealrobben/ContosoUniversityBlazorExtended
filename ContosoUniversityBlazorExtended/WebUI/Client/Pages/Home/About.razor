@page "/about"
@inject IStringLocalizer<StudentResources> StudentLocalizer
@inject IStringLocalizer<GeneralResources> GeneralLocalizer

<PageTitle>CU - About</PageTitle>

<h2>@StudentLocalizer["StudentBodyStatistics"]</h2>

@if (aboutInfo == null)
{
    <div class="d-flex justify-content-center">
        <MudProgressCircular Color="Color.Primary" Indeterminate="true" />
    </div>
}
else
{
    <MudTabs Elevation="2" Rounded="true" ApplyEffectsToContainer="true" PanelClass="pa-6">
        <MudTabPanel Text="@GeneralLocalizer["List"]">
            <MudText>
                <table class="table">
                    <tr>
                        <th>
                            @StudentLocalizer["EnrollmentDate"]
                        </th>
                        <th>
                            @StudentLocalizer["Students"]
                        </th>
                    </tr>

                    @if (aboutInfo != null)
                    {
                        @foreach (var item in aboutInfo.Items)
                        {
                            <tr>
                                <td>
                                    @item.EnrollmentDate?.ToShortDateString()
                                </td>
                                <td>
                                    @item.StudentCount
                                </td>
                            </tr>
                        }
                    }

                </table>
            </MudText>
        </MudTabPanel>
        <MudTabPanel Text="@GeneralLocalizer["Chart"]">
            <MudText>
                <MudChart ChartType="ChartType.Line" ChartSeries="@Series" XAxisLabels="@XAxisLabels" Width="100%"></MudChart>
            </MudText>
        </MudTabPanel>
    </MudTabs>
}
